<% provide(:title, "Facility Log in") %>
<div class="login forms">
  <h1>Facility Log in</h1>

    <div class="form">
      <%= form_with(url: facilities_login_path, scope: :session, local: true) do |f| %>

        <%= f.label :email %>
        <%= f.email_field :email, class: 'form-control' %>

        <%= f.label :password %>
        <%= f.password_field :password, class: 'form-control' %>

        <%= f.submit "Log in", class: "btn btn-primary" %>
      <% end %>

      <p>未登録の方 <%= link_to "こちらから", facilities_signup_path %></p>
    </div>
</div>